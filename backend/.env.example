# Database
DATABASE_URL="postgresql://user:password@localhost:5432/detramites?schema=public"

# JWT
JWT_SECRET="tu_secreto_super_seguro_aqui_cambialo"
JWT_EXPIRES_IN="24h"

# Server
PORT=4000
NODE_ENV="production"

# IMPORTANTE:
# 1. Copia este archivo a .env y llena los valores
# 2. Genera un JWT_SECRET seguro:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 3. NUNCA subas el archivo .env al repositorio
